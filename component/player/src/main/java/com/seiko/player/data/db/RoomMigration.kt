package com.seiko.player.data.db

import androidx.room.migration.Migration
import androidx.sqlite.db.SupportSQLiteDatabase

object RoomMigration {

//    val MIGRATION_1_2 get() = object : Migration(1, 2) {
//        override fun migrate(database: SupportSQLiteDatabase) {
//            database.execSQL("DELETE FROM Danma_table")
//            database.execSQL("CREATE TABLE IF NOT EXISTS VideoMatch_table (`_id` INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, `videoMd5` TEXT NOT NULL, `animeId` INTEGER NOT NULL, `episodeId` INTEGER NOT NULL, `isMatched` INTEGER NOT NULL)")
//        }
//    }

}